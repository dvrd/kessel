#!/usr/bin/env node
// ES2023 \u2014 HashbangComment is now part of the formal grammar and lands on
// Program.hashbang (ESTree). The value is the text AFTER `#!` and before
// the first LineTerminator.
const greeting = "hello";
console.log(greeting);
