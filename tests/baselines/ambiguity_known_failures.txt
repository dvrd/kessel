# Ambiguity known failures — fixtures that still diverge from OXC in deep
# compare. This file is documentation and a reviewer aid; the unit golden
# gate (`task test`) already passes on every entry because the goldens
# encode kessel's tsx-mode interpretation. The "still-diverging" list
# below comes from running `verify_json_deep.js PARSER=oxc` against each
# fixture in `spec/ambiguity/`.
#
# Previously tracked (now passing — parser improvements landed, kept here
# only as historical context):
#   - spec/ambiguity/003_generic_call_vs_relational.js
#   - spec/ambiguity/007_type_arguments_call_chain.js
#   - spec/ambiguity/010_import_type_vs_import_call.js
#   - spec/ambiguity/005_jsx_attribute_nested_element.js
#   - spec/ambiguity/006_jsx_expression_nested_generic_like.js
#   - spec/ambiguity/008_less_than_binary_not_generic.js
#   - spec/ambiguity/009_jsx_fragment_vs_type_context.js

# Still-diverging fixtures. Today both kessel AND OXC reject the input in
# tsx mode (each reports 1 parse error). The deep-compare divergence is
# *not* a parser-correctness gap — it is a recovery-shape difference:
#
#   * OXC drops the partial AST when a tsx-mode parse fails: it returns
#     `program.body = []` with `start: 0, end: 0`.
#   * Kessel keeps the partial AST it had reconstructed up to the error,
#     plus an `errors: [...]` field on the Program node so downstream
#     tooling (IDE highlighters, recovery-based formatters) can still
#     work with the structure.
#
# Either shape is defensible. Both engines agree the input is invalid.
# These entries stay listed so the deep-compare runner does not flag a
# reporting-style choice as a regression, and so reviewers know there is
# no actual parser bug to chase.

spec/ambiguity/001_ts_assertion_vs_jsx_simple.js   # `<Type>expr`        — both reject; kessel keeps partial body.
spec/ambiguity/002_ts_assertion_vs_jsx_paren.js    # `<Type>(expr)`      — both reject; kessel keeps partial body.
spec/ambiguity/004_generic_arrow_vs_relational.js  # `<T>(value: T) => …` — both reject; kessel keeps partial body.
