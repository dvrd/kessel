# Ambiguity known failures — fixtures that still diverge from OXC or hit parser errors.
# This file is documentation and a reviewer aid. The verifier consumes it to label
# current outcomes, not to skip execution.
#
# Previously tracked (now passing — parser improvements landed, leaving them
# here only as historical context; verify_ambiguity classifies them as `pass`
# because they match both the golden and the deep compare now):
#   - spec/ambiguity/003_generic_call_vs_relational.js
#   - spec/ambiguity/007_type_arguments_call_chain.js
#   - spec/ambiguity/010_import_type_vs_import_call.js
#
# Still-diverging fixtures:

spec/ambiguity/004_generic_arrow_vs_relational.js  # Generic arrow recovery still emits parse errors.

# The following six were passing with a tsx-interpretation golden generated by
# run_tests.sh, but verify_ambiguity parses each in its per-fixture "intent"
# mode (ts/jsx/js) which now produces a different AST than the tsx-mode
# golden. Both gates are correct for their own question — lifting these to
# pass requires aligning the two gates on a single lang choice per fixture.
spec/ambiguity/001_ts_assertion_vs_jsx_simple.js
spec/ambiguity/002_ts_assertion_vs_jsx_paren.js
spec/ambiguity/005_jsx_attribute_nested_element.js
spec/ambiguity/006_jsx_expression_nested_generic_like.js
spec/ambiguity/008_less_than_binary_not_generic.js
spec/ambiguity/009_jsx_fragment_vs_type_context.js
